/* @vercel/og */ import { ImageResponse } from 'next/og'
export const runtime='edge';export const alt='Calao Pronostic';export const size={width:1200,height:630};export const contentType='image/png'
export async function GET(){const logo=new URL('/logo-calao.png',process.env.APP_URL).toString();return new ImageResponse((<div style={{width:'100%',height:'100%',display:'flex',alignItems:'center',justifyContent:'center',background:'radial-gradient(1200px 700px at 10% -10%, #1B1A32 0%, #0B0A12 60%)',position:'relative'}}><svg width="1200" height="630" style={{position:'absolute',inset:0,opacity:.35}}>{Array.from({length:300}).map((_,i)=>(<circle key={i} cx={Math.random()*1200} cy={Math.random()*630} r={Math.random()*1.8+0.2} fill="#ffffff" />))}</svg><div style={{position:'absolute',inset:0,filter:'blur(30px)',opacity:.7,background:'conic-gradient(from 30deg at 30% 10%, #7C3AED, transparent 20%, #22D3EE 35%, transparent 60%, #F472B6 80%, transparent)'}} /><div style={{display:'flex',alignItems:'center',gap:32,flexDirection:'column'}}><img src={logo} width={140} height={140} style={{filter:'drop-shadow(0 0 20px rgba(124,58,237,.6))'}} /><div style={{display:'flex',flexDirection:'column',alignItems:'center'}}><div style={{fontSize:56,fontWeight:800,color:'#E7E6F9'}}>Calao Pronostic</div><div style={{fontSize:26,color:'#B7B2D6'}}>La différence se joue sur la lecture du jeu.</div></div></div></div>),{...size})}
